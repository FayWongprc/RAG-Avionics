# 评估功能 V2 改进说明

## 🎯 改进内容

基于你的反馈，我对评估维度3进行了重大改进：

### 改进1：在评估 prompt 中包含原子需求信息

**之前的问题**:
- LLM 只看到测试用例本身，不知道要验证什么需求
- 无法判断测试用例是否真正验证了需求的核心约束

**现在的改进**:
```python
# 评估 prompt 现在包含：
- 原子需求ID
- 需求类别（功能/性能/安全等）
- 需求陈述（完整的需求描述）
- 测试用例的所有信息
```

**效果**:
- LLM 可以判断测试用例是否正确验证了需求
- 评分更准确，理由更有针对性

### 改进2：评估每个原子需求的测试覆盖完整性

**新增功能**:
- 评估每个原子需求是否被充分测试
- 判断是否需要补充其他测试方法
- 给出具体的补充建议

**评估内容**:

1. **已覆盖的测试方法**: 列出当前已有的测试方法

2. **必选方法检查**: 
   - 正常范围测试（必选）
   - 健壮性测试（必选）

3. **条件方法检查**（根据需求特征判断）:
   - **边界值分析**: 需求是否包含明确的数值阈值？
   - **逻辑与判定测试**: 需求是否包含多个条件组合（AND/OR/NOT）？
   - **状态与场景迁移测试**: 需求是否涉及系统模式或状态？

4. **缺失方法**: 列出应该补充但尚未覆盖的测试方法

5. **补充建议**: 对于每个缺失的方法，给出具体的测试用例设计建议

**输出格式**:
```json
{
  "atomic_req_id": "AR-001",
  "covered_methods": ["正常范围测试", "健壮性测试"],
  "missing_required_methods": [],
  "missing_conditional_methods": ["边界值分析"],
  "is_complete": false,
  "completeness_score": 75,
  "suggestions": [
    {
      "method": "边界值分析",
      "reason": "需求包含明确阈值 100ms，应测试边界值",
      "test_case_suggestion": "设计测试用例验证 99ms、100ms、101ms 三个边界点"
    }
  ]
}
```

## 📋 系统使用的五种测试方法

### 1. 正常范围测试（等价类划分）【必选】
- 从有效输入域中选取标称值
- 验证正常工况下的预期行为

### 2. 健壮性测试（异常容错验证）【必选】
- 使用无效输入（超量程、信号丢失、数据无效NCD）
- 验证告警或容错处理机制

### 3. 边界值分析（BVA）【条件适用】
- **适用条件**: 需求包含明确的数值阈值 T
- 测试 T-Δ、T、T+Δ

### 4. 逻辑与判定测试（类MC/DC思路）【条件适用】
- **适用条件**: 需求包含多个条件组合（AND/OR/NOT）
- 覆盖判定结果独立翻转的组合

### 5. 状态与场景迁移测试【条件适用】
- **适用条件**: 需求涉及明确的系统模式、运行状态
- 测试不同状态下的行为

## 🔄 评估流程

### 第一步：静态分析（快速）
- 检查每个需求是否包含正常测试和健壮性测试
- 计算方法覆盖率

### 第二步：LLM 盲评测试用例逻辑（可选）
- 对每个测试用例进行评分（1-5分）
- 评估测试步骤是否符合声明的测试方法
- 评估是否正确验证了原子需求
- 返回评分理由和具体问题列表

### 第三步：LLM 评估需求覆盖完整性（可选）
- 对每个原子需求进行分析
- 判断测试方法覆盖是否完整
- 给出补充建议

## 📊 前端展示建议

### 1. 测试用例评分表

```
┌─────────────────────────────────────────────────────────────┐
│ 测试用例逻辑合理性评分                                       │
├──────────┬───────┬────────────────────┬─────────────────────┤
│ 用例ID   │ 评分  │ 评分理由           │ 具体问题            │
├──────────┼───────┼────────────────────┼─────────────────────┤
│ TC-001   │ 5/5   │ 完全符合正常范围   │ 无问题              │
│          │       │ 测试的定义         │                     │
├──────────┼───────┼────────────────────┼─────────────────────┤
│ TC-004   │ 2/5   │ 声明为正常范围测试 │ • 测试方法标注错误  │
│          │       │ 但使用了超限输入   │ • 输入值超出范围    │
│          │       │                    │ • 预期结果不符      │
└──────────┴───────┴────────────────────┴─────────────────────┘
```

### 2. 需求覆盖完整性分析

```
┌─────────────────────────────────────────────────────────────┐
│ 原子需求: AR-002                                            │
│ 需求陈述: 输入信号范围为 0-100，超出范围应触发告警          │
├─────────────────────────────────────────────────────────────┤
│ 完整性评分: 75%                                             │
│                                                             │
│ ✅ 已覆盖方法:                                              │
│   • 正常范围测试                                            │
│   • 健壮性测试                                              │
│                                                             │
│ ⚠️  缺失方法:                                               │
│   • 边界值分析                                              │
│                                                             │
│ 💡 补充建议:                                                │
│   方法: 边界值分析                                          │
│   原因: 需求包含明确的数值范围 [0, 100]                     │
│   建议: 设计测试用例验证以下边界点：                        │
│         - 输入 -1（刚好低于下界）                           │
│         - 输入 0（下界）                                    │
│         - 输入 100（上界）                                  │
│         - 输入 101（刚好超过上界）                          │
└─────────────────────────────────────────────────────────────┘
```

### 3. 总览仪表盘

```
┌──────────────────────────────────────────────────────────────┐
│  评估总览                                                     │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐        │
│  │需求覆盖率│ │结构合格率│ │方法覆盖率│ │链路有效率│        │
│  │  95.2%   │ │  98.5%   │ │  87.3%   │ │  100%    │        │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘        │
│                                                               │
│  LLM 评估结果:                                                │
│  • 平均用例评分: 4.2/5.0                                      │
│  • 平均需求完整性: 82%                                        │
│  • 需要补充测试的需求: 3 个                                   │
└──────────────────────────────────────────────────────────────┘
```

## 🚀 使用方法

### API 调用

```python
POST /api/evaluate
{
  "result": {...},              # 分析结果
  "enable_llm": true,           # 启用 LLM 评估
  "llm_provider": "qwen",
  "llm_model": "qwen3-max"
}
```

### 响应格式

```json
{
  "logic": {
    "llm_scores": [
      {
        "tc_id": "TC-001",
        "atomic_req_id": "AR-001",
        "score": 5,
        "reasoning": "完全符合正常范围测试的定义",
        "issues": []
      }
    ],
    "average_llm_score": 4.2,
    "requirement_coverage_analysis": [
      {
        "atomic_req_id": "AR-002",
        "covered_methods": ["正常范围测试", "健壮性测试"],
        "missing_required_methods": [],
        "missing_conditional_methods": ["边界值分析"],
        "is_complete": false,
        "completeness_score": 75,
        "suggestions": [...]
      }
    ]
  }
}
```

## 📝 注意事项

1. **性能**: LLM 评估需要时间
   - 测试用例评分: 每个用例 1-3 秒
   - 需求覆盖分析: 每个需求 2-5 秒
   - 建议先运行快速评估，再根据需要启用 LLM

2. **准确性**: LLM 评估依赖于提示词质量
   - 已包含详细的测试方法定义
   - 已包含评分标准和示例
   - 评估结果仅供参考，最终判断需要人工确认

3. **成本**: LLM 调用会产生 API 费用
   - 建议在关键节点使用
   - 可以只对低分用例进行深度分析

## 🧪 测试

```bash
# 基础测试（不使用 LLM）
python test_evaluation.py

# 完整测试（包含 LLM）
python test_evaluation_with_llm.py
```

## 📚 相关文档

- [评估模块详细文档](rag_avionics/evaluation/README.md)
- [前端集成指南](rag_avionics/evaluation/FRONTEND_INTEGRATION.md)
- [提示词设计](rag_avionics/evaluation/prompts.py)
