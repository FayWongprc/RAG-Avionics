"""
评估模块的提示词模板
"""

# 测试方法与逻辑合理性评估提示词
TEST_LOGIC_EVALUATION_PROMPT = """你是一位资深的航空软件测试专家，负责评估测试用例的逻辑合理性。

请评估以下测试用例的**测试步骤和预期结果**是否真正符合它**声明的测试方法**，以及是否正确验证了**目标原子需求**。

## 目标原子需求

**原子需求ID**: {atomic_req_id}

**需求类别**: {atomic_req_category}

**需求陈述**: {atomic_req_statement}

## 测试用例信息

**测试用例ID**: {tc_id}

**声明的测试方法**: {test_method}

**测试目的**: {objective}

**设计理由**: {design_rationale}

**前提条件**:
{preconditions}

**输入/刺激**:
{inputs}

**测试步骤**:
{steps}

**预期结果**:
{expected_results}

## 系统使用的五种测试方法

### 1. 正常范围测试（等价类划分）【必选】
- **定义**: 从有效输入域中选取标称值，验证正常工况下的预期行为
- **输入特征**: 典型的、正常的输入值
- **预期结果**: 系统正常工作，符合规范

### 2. 健壮性测试（异常容错验证）【必选】
- **定义**: 使用无效输入，验证告警或容错处理机制
- **输入特征**: 超量程、信号丢失、数据无效(NCD)、非预期状态
- **预期结果**: 系统检测异常、触发告警、进入安全状态、不崩溃

### 3. 边界值分析（BVA）【条件适用】
- **定义**: 测试数值约束或物理量阈值的边界点
- **适用条件**: 需求包含明确的数值阈值 T
- **输入特征**: T-Δ（刚好低于）、T（刚好等于）、T+Δ（刚好超过）
- **预期结果**: 边界处的正确行为

### 4. 逻辑与判定测试（类MC/DC思路）【条件适用】
- **定义**: 测试多个条件组合，覆盖判定结果独立翻转
- **适用条件**: 需求包含多个条件组合（AND/OR/NOT）
- **输入特征**: 不同条件组合
- **预期结果**: 每个条件独立影响判定结果

### 5. 状态与场景迁移测试【条件适用】
- **定义**: 测试系统在不同模式或状态下的行为
- **适用条件**: 需求涉及明确的系统模式、运行状态
- **输入特征**: 不同的系统状态（如空中/地面模式）
- **预期结果**: 状态迁移正确，行为符合当前状态

## 评估要点

### 评估点1：测试方法选择是否合理
- 根据原子需求的特征，判断选择的测试方法是否合适
- 例如：如果需求包含明确阈值，应该使用边界值分析
- 例如：如果需求包含多个条件，应该使用逻辑与判定测试

### 评估点2：测试步骤是否符合方法定义
- 输入值的选择是否符合测试方法的要求
- 测试步骤是否能够验证该方法要验证的内容
- 预期结果是否准确反映该测试方法应验证的行为

### 评估点3：是否正确验证了原子需求
- 测试用例是否真正验证了原子需求的核心约束
- 预期结果是否与需求陈述一致

## 评分规则 (1-5分)

**5分 - 完全符合**:
- 测试步骤与声明的测试方法**完全一致**
- 输入值的选择**精准匹配**测试方法的要求
- 预期结果**准确反映**该测试方法应验证的行为
- 逻辑清晰，无歧义

**4分 - 基本符合**:
- 测试步骤与声明的测试方法**大体一致**
- 输入值选择合理，但可能不够精确
- 预期结果正确，但描述可能不够详细
- 存在小的改进空间

**3分 - 部分符合**:
- 测试步骤与声明的测试方法**有一定关联**
- 输入值选择不够精确，或缺少关键测试点
- 预期结果不够明确，或遗漏重要验证点
- 存在明显的改进空间

**2分 - 不太符合**:
- 测试步骤与声明的测试方法**关联较弱**
- 输入值选择不当，未能体现测试方法的核心
- 预期结果模糊或不正确
- 需要大幅修改

**1分 - 完全不符**:
- 测试步骤与声明的测试方法**完全不匹配**
- 输入值选择错误，或测试方法标注错误
- 预期结果与测试方法无关
- 需要重新设计

## 常见问题示例

### ❌ 问题1：声明"健壮性测试"，但使用正常输入
```
声明: 健壮性测试
输入: 正常范围内的值 (50)
预期: 系统正常工作
问题: 应该使用超出范围的值，预期应该是错误处理
```

### ❌ 问题2：声明"正常范围测试"，但测试异常情况
```
声明: 正常范围测试
输入: 超出范围的值 (999)
预期: 系统报错
问题: 应该使用正常值，预期应该是正常工作
```

### ❌ 问题3：声明"边界值测试"，但未测试边界
```
声明: 边界值测试
输入: 中间值 (50)，范围是 [0, 100]
预期: 系统正常工作
问题: 应该测试 0、100 等边界值
```

### ✅ 正确示例：健壮性测试
```
声明: 健壮性测试
输入: 超出范围的值 (150)，范围是 [0, 100]
预期: 系统检测到输入超限，触发告警，拒绝处理
```

## 输出格式

请以 JSON 格式返回评估结果：

```json
{{
  "score": 1-5的整数,
  "reasoning": "简短的评分理由（50字以内）",
  "issues": [
    "具体问题1（如果有）",
    "具体问题2（如果有）"
  ]
}}
```

**注意**:
- `score`: 必须是 1-5 的整数
- `reasoning`: 简明扼要地说明评分依据
- `issues`: 如果评分 < 5，必须列出具体问题；如果评分 = 5，可以为空数组

现在开始评估上述测试用例。
"""


# 原子需求测试覆盖完整性评估提示词
REQUIREMENT_COVERAGE_EVALUATION_PROMPT = """你是一位资深的航空软件测试专家，负责评估原子需求的测试覆盖完整性。

请评估以下原子需求的测试用例覆盖是否完整，并判断是否需要补充其他测试方法。

## 原子需求信息

**原子需求ID**: {atomic_req_id}

**需求类别**: {atomic_req_category}

**需求陈述**: {atomic_req_statement}

**来源文本**: {atomic_req_source_text}

## 当前测试用例覆盖情况

{test_cases_summary}

## 系统使用的五种测试方法

1. **正常范围测试（等价类划分）**【必选】
   - 从有效输入域中选取标称值，验证正常工况

2. **健壮性测试（异常容错验证）**【必选】
   - 使用无效输入，验证告警或容错处理

3. **边界值分析（BVA）**【条件适用】
   - 仅当需求包含明确的数值阈值时使用
   - 测试 T-Δ、T、T+Δ

4. **逻辑与判定测试（类MC/DC思路）**【条件适用】
   - 仅当需求包含多个条件组合（AND/OR/NOT）时使用

5. **状态与场景迁移测试**【条件适用】
   - 仅当需求涉及明确的系统模式、运行状态时使用

## 评估任务

请分析：

1. **已覆盖的测试方法**: 列出当前已有的测试方法
2. **必选方法检查**: 正常范围测试和健壮性测试是否都已覆盖
3. **条件方法检查**: 根据需求特征，判断是否需要以下方法：
   - 边界值分析：需求是否包含明确的数值阈值？
   - 逻辑与判定测试：需求是否包含多个条件组合？
   - 状态与场景迁移测试：需求是否涉及系统模式或状态？
4. **缺失方法**: 列出应该补充但尚未覆盖的测试方法
5. **补充建议**: 对于每个缺失的方法，给出具体的测试用例设计建议

## 输出格式

请以 JSON 格式返回评估结果：

```json
{{
  "covered_methods": ["已覆盖的测试方法列表"],
  "missing_required_methods": ["缺失的必选方法"],
  "missing_conditional_methods": ["缺失的条件方法"],
  "is_complete": true/false,
  "completeness_score": 0-100的整数,
  "suggestions": [
    {{
      "method": "缺失的测试方法名称",
      "reason": "为什么需要这个方法",
      "test_case_suggestion": "具体的测试用例设计建议"
    }}
  ]
}}
```

**注意**:
- `covered_methods`: 当前已有测试用例使用的方法
- `missing_required_methods`: 缺失的必选方法（正常范围测试、健壮性测试）
- `missing_conditional_methods`: 根据需求特征应该有但缺失的条件方法
- `is_complete`: 是否完整覆盖（必选方法都有，且条件方法按需覆盖）
- `completeness_score`: 完整性评分（0-100）
- `suggestions`: 补充建议列表，每个建议包含方法名、原因和具体设计建议

现在开始评估上述原子需求的测试覆盖完整性。
"""
