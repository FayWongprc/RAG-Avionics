# ── 固定规则层：原子需求分解黄金准则 ─────────────────────

ATOMIC_REQ_GOLDEN_RULES = """
一条合格的"原子需求"必须同时满足以下特性：

1. 单一性 (Singularity)
   一条需求只表达一个可独立验证的约束或行为。
   若一句话中出现"并且/同时/以及"连接两个不同动作或条件，应拆为多条。

2. 无歧义性 (Unambiguity)
   不使用"适当的""合理的""尽快"等模糊词汇。
   所有阈值、时限、状态必须有明确的数值或枚举定义。

3. 可测试性 (Testability)
   每条需求必须能设计出至少一个测试用例来判定通过/失败。
   需求中必须包含可观测的输入条件和期望输出/行为。

4. 可追溯性 (Traceability)
   每条原子需求必须能追溯到原始需求编号和来源文本。

5. 完整性 (Completeness)
   关键约束不能遗漏：触发条件、禁止条件、时序约束、告警行为、边界值。

6. 一致性 (Consistency)
   原子需求之间不能相互矛盾。
""".strip()


# ── 动态领域层：提取术语用于本地词典匹配 ──────────────────────────

DOMAIN_QUERY_PROMPT = """
你是一个资深的机载软件适航审查专家。你的任务是从机载软件高层需求（HLR）文本中，精准提取需要进行领域消歧的专业术语。

【提取规则】（必须严格遵守，宁缺毋滥）：
1. 提取全大写英文缩写、项目代号（如FSECU, IDAL A, ARINC 429, WOW）以及带有下划线的特定信号标识后缀（如 Y_CCDL, XX_CBDL, XY）。
2. 提取特定的机载系统专有组件中英文或中文名称（如“FSECU硬件”、“引导加载软件”）
3. 绝对严禁提取带有具体数值的物理单位（如 20Hz, 50ms, 28VDC，这些属于通用物理常识，不需要消歧）。
4. 绝对严禁提取通用名词或动作（例如：系统、飞机、需求、控制、软件）。

待提取的原始需求文本：
{requirement_text}

【输出格式要求】
必须严格输出一个合法的 JSON 字符串数组，不要包含任何 markdown 代码块标记（如 ```json），也不要任何解释性文字。
如果未发现符合上述严苛规则的专业术语，请直接输出空数组 []。

示例输出格式：
["FSECU", "IDAL A"，"FSECU硬件"]
""".strip()


# 带预检索与少样本示例的增强提示词
DECOMPOSE_WITH_GUIDANCE_PROMPT = """
你是机载软件需求工程与测试工程专家。你现在的任务是将用户输入的原始需求分解为可测试的原子需求。
══════════════════════════════════════════
第一部分：原子需求黄金准则
══════════════════════════════════════════
{golden_rules}
══════════════════════════════════════════
第二部分：领域术语与约束
══════════════════════════════════════════
以下是从知识库中检索到的与本需求相关的专业术语定义、协议约束、技术参数等。
请在拆分时使用这些准确的定义，不要臆造或偏离标准中的含义：
{domain_text}
══════════════════════════════════════════
第三部分：参考示例 (Few-Shot Example)
══════════════════════════════════════════
示例输入需求：
"当系统处于空中状态（WOW=False）且雷达高度低于2000英尺时，系统应触发'低高度'告警，并向FDR（飞行数据记录器）发送状态事件。"

正确拆解的 JSON 输出示例：
{{
  "atomic_requirements": [
    {{
      "req_id": "AR-001",
      "source_req": "REQ-DEMO",
      "source_text": "当系统处于空中状态（WOW=False）且雷达高度低于2000英尺时，系统应触发'低高度'告警",
      "statement": "当系统处于空中状态（WOW=False）且雷达高度低于2000英尺时，系统应触发'低高度'告警。",
      "category": "告警"
    }},
    {{
      "req_id": "AR-002",
      "source_req": "REQ-DEMO",
      "source_text": "并向FDR（飞行数据记录器）发送状态事件。",
      "statement": "当系统处于空中状态（WOW=False）且雷达高度低于2000英尺时，系统应向FDR（飞行数据记录器）发送状态事件。",
      "category": "接口"
    }}
  ]
}}

══════════════════════════════════════════
任务
══════════════════════════════════════════
请把用户给出的"需求段落"拆分为若干条"原子需求"，严格遵循上述黄金准则：
- 每条原子需求必须满足：单一性、无歧义性、可测试性、可验证性
- 避免把两个动作/输出混在一条；禁止把复合前置条件（AND/OR）强行拆离。
- 需要保留关键约束（阈值、单位、时序、触发条件、禁止条件、告警条件）
- 使用【第二部分】中的准确术语定义，不要偏离标准含义

追溯要求（必须满足）：
- 每条原子需求必须填写 source_req（原始需求编号），若输入中能识别出需求编号则使用它，否则写 "UNKNOWN"
- 每条原子需求必须填写 source_text：从原始需求段落中截取的"直接来源句/条款"（尽量原文拷贝）

输出必须是严格 JSON（不要使用 markdown 代码块），格式参考上述示例。

已识别的原始需求编号（可能为 UNKNOWN）：
{requirement_id}

需求段落：
{requirement_text}
""".strip()


# ── 测试用例生成提示词（含 DO-178C 动态测试策略、防幻觉约束与多维少样本示例） ────────────

GENERATE_TESTCASES_PROMPT = """
你是资深的航空软件测试工程师兼DO-178C审查员。你将获得：
1) 一条"原子需求"（包含 source_req/source_text）
2) 从标准/规范或需求文档检索到的"证据片段"（用于引用与支撑）

请基于 IEEE 829 风格生成测试用例。
【重要核心原则：动态方法选择】你不必对该需求生搬硬套所有测试方法！请先深刻分析需求特征，并【强制】遵循以下策略进行按需设计：

1. 正常范围测试（等价类划分）【必选】：
   根据需求约束识别有效输入域。从等价类中选取标称值，验证正常工况下的预期行为。

2. 健壮性测试（异常容错验证）【必选】：
   必须设计至少一个无效输入（如：超量程、信号丢失、数据无效NCD、非预期状态），验证告警或容错处理机制。

3. 边界值分析（BVA）【条件适用】：
   仅当需求中包含明确的“数值约束或物理量阈值（T）”时使用。必须生成刚好达到、低于 (T-Δ) 及超过 (T+Δ) 的用例。

4. 逻辑与判定测试（类MC/DC思路）【条件适用】：
   仅当需求包含多个条件组合（AND/OR/NOT）时使用。设计用例覆盖判定结果独立翻转的组合。

5. 状态与场景迁移测试【条件适用】：
   仅涉及明确系统模式、运行状态（如空中/地面模式，上电初始化）时使用。

══════════════════════════════════════════
【参考示例 (Multi-Dimensional Few-Shot Example)】
为了让你明确设计深度，假设输入的原子需求为："当系统处于飞行状态（WOW=False）且雷达高度持续低于2000英尺时，系统应触发低高度告警（Low_Alt_Alert=True）。"
你生成的 `test_cases` 数组应当像专业工程师一样，从不同维度覆盖该需求，示例如下：

[
  {{
    "tc_id": "TC-DEMO-001",
    "title": "雷达高度刚好低于阈值时的告警触发验证（下边界）",
    "test_method": "边界值分析",
    "design_rationale": "需求包含明确阈值(2000英尺)且条件为'低于'。假设高度最小工程精度Δ=1英尺，选取 T-Δ = 1999 英尺。验证系统在刚越界时能正确响应。",
    "objective": "验证飞行状态下，雷达高度刚降至1999英尺时触发告警。",
    "preconditions": ["系统处于飞行状态 (WOW = False)"],
    "inputs": ["雷达高度 Rad_Alt = 1999 英尺"],
    "steps": ["1. 维持 WOW = False", "2. 注入雷达高度信号 Rad_Alt = 1999"],
    "expected_results": ["系统输出 Low_Alt_Alert = True"],
    "postconditions": ["无"],
    "trace_to_atomic_req": "AR-DEMO",
    "trace_to_source_req": "REQ-DEMO",
    "evidence_refs": ["Demo_Spec.pdf | p.45"]
  }},
  {{
    "tc_id": "TC-DEMO-002",
    "title": "雷达高度信号无效时的容错处理",
    "test_method": "健壮性测试",
    "design_rationale": "DO-178C要求验证异常输入。模拟雷达高度计总线通讯中断或发送无效数据(NCD)，系统不应因数据异常或突变而产生误告警。",
    "objective": "验证雷达高度信号无效时，系统不会触发虚假告警。",
    "preconditions": ["系统处于飞行状态 (WOW = False)"],
    "inputs": ["雷达高度 Rad_Alt 信号状态字 = 无效 (Invalid/NCD)"],
    "steps": ["1. 维持 WOW = False", "2. 将 Rad_Alt 信号源设置为无效状态"],
    "expected_results": ["Low_Alt_Alert 保持 False，系统不上报低高度告警"],
    "postconditions": ["系统记录传感器故障日志"],
    "trace_to_atomic_req": "AR-DEMO",
    "trace_to_source_req": "REQ-DEMO",
    "evidence_refs": ["Demo_Spec.pdf | p.45"]
  }},
  {{
    "tc_id": "TC-DEMO-003",
    "title": "地面状态下高度低于阈值的逻辑阻断验证",
    "test_method": "状态与场景迁移测试",
    "design_rationale": "需求指定了前置条件（飞行状态 WOW=False）。必须验证在前置状态不满足（即地面状态 WOW=True）时，即使高度满足低于2000英尺，告警逻辑也被安全隔离。",
    "objective": "验证地面状态下，雷达高度低于2000英尺时告警逻辑不触发。",
    "preconditions": ["系统处于地面状态 (WOW = True)"],
    "inputs": ["雷达高度 Rad_Alt = 1500 英尺"],
    "steps": ["1. 设定并维持 WOW = True", "2. 注入雷达高度信号 Rad_Alt = 1500"],
    "expected_results": ["Low_Alt_Alert 严格保持 False"],
    "postconditions": ["无"],
    "trace_to_atomic_req": "AR-DEMO",
    "trace_to_source_req": "REQ-DEMO",
    "evidence_refs": ["Demo_Spec.pdf | p.45"]
  }}
]
══════════════════════════════════════════

【异常兜底约束】
仅当提供的"证据片段"完全为空（没有任何证据）时，你才需要在生成的 `title` 末尾加上 `[WARNING: 证据缺失需人工复核]`。如果证据片段存在但内容有限，你应该基于现有证据和原子需求尽力设计测试用例，不需要加警告。

输出必须是严格 JSON（不要使用 markdown 代码块），格式如下：
{{
  "test_cases": [
    {{
      "tc_id": "TC-...",
      "title": "...",
      "test_method": "正常范围/边界值/逻辑组合/状态迁移/健壮性",
      "design_rationale": "解释选用该方法的原因。如果是边界值，说明Δ取值依据（参考上述示例）。",
      "objective": "...",
      "preconditions": ["..."],
      "inputs": ["..."],
      "steps": ["..."],
      "expected_results": ["..."],
      "postconditions": ["..."],
      "trace_to_atomic_req": "AR-...",
      "trace_to_source_req": "...",
      "evidence_refs": ["..."]
    }}
  ]
}}

原子需求：
{atomic_requirement_json}

原始需求段落：
{requirement_text}

证据片段：
{evidence_text}
""".strip()
