# 双分块策略实现说明

## 改动概述

实现了针对不同文档类型的双分块策略：
- **标准文档**（Avionics_standards）：句子窗口检索
- **SRD 文档**（Avionics_srd）：固定大小分块

两种策略处理后的节点统一存入同一个 Qdrant 向量库，通过 `category` 元数据标签区分。

## 目录结构

```
data/
├── Avionics_standards/     # 航空标准文档（DO-178C、ARP4754A、IEEE 829 等）
│   ├── DO-178C.pdf
│   ├── ARP4754A.pdf
│   └── IEEE 829.pdf
├── Avionics_srd/           # 软件需求文档（SRD）
│   ├── 项目需求文档.docx
│   └── 系统需求规约.pdf
└── avionics_terms.json     # 术语词典
```

## 核心改动

### 1. settings.py

**新增配置：**
```python
@dataclass(frozen=True)
class AppPaths:
    root: Path
    kb_dir: Path
    srd_dir: Path  # 新增：SRD 目录
    storage_dir: Path
    qdrant_dir: Path

@dataclass(frozen=True)
class ModelSettings:
    # Sentence Window Retrieval (用于标准文档)
    sentence_window_size: int = 6
    
    # Fixed Chunking (用于 SRD 软件需求文档)
    srd_chunk_size: int = 512      # 大块策略，确保逻辑段落完整
    srd_chunk_overlap: int = 100   # 重叠度，防止因果链在边缘处丢失
    
    top_k: int = 6
```

### 2. indexing.py

**双分块策略实现：**

```python
def build_or_load_index(...) -> VectorStoreIndex:
    all_nodes = []
    
    # 1. 处理标准文档（句子窗口检索）
    if paths.kb_dir.exists():
        standard_docs = SimpleDirectoryReader(paths.kb_dir).load_data()
        for doc in standard_docs:
            doc.metadata["category"] = "Standards"
        
        standard_parser = SentenceWindowNodeParser.from_defaults(
            window_size=ms.sentence_window_size,
            window_metadata_key="window",
            original_text_metadata_key="original_sentence",
        )
        standard_nodes = standard_parser.get_nodes_from_documents(standard_docs)
        all_nodes.extend(standard_nodes)
    
    # 2. 处理 SRD 文档（固定大小分块）
    if paths.srd_dir.exists():
        srd_docs = SimpleDirectoryReader(paths.srd_dir).load_data()
        for doc in srd_docs:
            doc.metadata["category"] = "SRD_Context"
        
        srd_parser = SentenceSplitter(
            chunk_size=ms.srd_chunk_size,
            chunk_overlap=ms.srd_chunk_overlap,
            separator="\n\n"  # 优先按段落切分
        )
        srd_nodes = srd_parser.get_nodes_from_documents(srd_docs)
        all_nodes.extend(srd_nodes)
    
    # 3. 统一构建索引
    index = VectorStoreIndex(nodes=all_nodes, storage_context=storage_context)
    return index
```

## 分块策略对比

| 特性 | 标准文档（Standards） | SRD 文档（SRD_Context） |
|------|---------------------|----------------------|
| **分块器** | SentenceWindowNodeParser | SentenceSplitter |
| **分块粒度** | 单句（精准匹配） | 512 字符（大块） |
| **上下文** | 前后各 6 句窗口 | 100 字符重叠 |
| **适用场景** | 条款式标准文档 | 叙述性需求文档 |
| **优势** | 精准检索，避免噪声 | 保持逻辑完整性 |
| **元数据标签** | `category="Standards"` | `category="SRD_Context"` |

## 使用方法

### 1. 准备文档

```bash
# 标准文档放入 Avionics_standards
data/Avionics_standards/
├── DO-178C.pdf
├── ARP4754A.pdf

# SRD 文档放入 Avionics_srd
data/Avionics_srd/
├── 软件需求规约.docx
└── 系统需求文档.pdf
```

### 2. 重建向量库

```bash
streamlit run streamlit_app.py
```

在侧边栏勾选"重建向量库"，系统会：
1. 删除旧的向量库
2. 使用句子窗口策略处理标准文档
3. 使用固定分块策略处理 SRD 文档
4. 将所有节点统一存入 Qdrant

### 3. 查看构建日志

```
======================================================================
开始构建向量索引（双分块策略）
======================================================================

[1/2] 处理标准文档: data/Avionics_standards
  策略: 句子窗口检索（窗口大小=6）
  ✓ 生成 1523 个节点

[2/2] 处理 SRD 文档: data/Avionics_srd
  策略: 固定大小分块（chunk_size=512, overlap=100）
  ✓ 生成 245 个节点

======================================================================
总计: 1768 个节点
======================================================================

正在构建向量索引...
✓ 向量索引构建完成
```

## 检索时的区分

虽然两种文档存在同一个向量库，但可以通过 `metadata["category"]` 区分：

```python
# 检索结果示例
{
    "text": "...",
    "metadata": {
        "category": "Standards",  # 或 "SRD_Context"
        "file_path": "...",
        "page": "..."
    }
}
```

## 注意事项

1. **必须重建向量库**：修改分块策略后，旧的向量库不兼容
2. **目录可选**：如果某个目录不存在或为空，系统会跳过并继续处理另一个
3. **至少一个目录有文档**：如果两个目录都为空，会抛出错误
4. **支持的文件格式**：PDF、DOCX、TXT、MD 等（需要相应的解析库）

