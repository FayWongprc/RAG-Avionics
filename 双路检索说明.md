# 双路检索（Dual Retrieval）实现说明

## 问题背景

在单路检索中，如果设置 `top_k=3`，可能会出现：
- 检索到的 3 条全部来自 SRD 文档
- 或全部来自标准文档
- 导致某一类证据缺失

## 解决方案：双路检索

使用元数据过滤器（MetadataFilters）强制在两个"专属泳道"中分别检索：

```
查询："起落架控制逻辑"
    ↓
┌─────────────────────────────────────┐
│  双路检索（Dual Retrieval）          │
├─────────────────────────────────────┤
│                                     │
│  泳道 1: 标准文档 (Standards)        │
│  ├─ 过滤器: category="Standards"    │
│  ├─ Top-K: 3                        │
│  └─ 结果: DO-178C 相关条款 3 条     │
│                                     │
│  泳道 2: SRD 文档 (SRD_Context)     │
│  ├─ 过滤器: category="SRD_Context"  │
│  ├─ Top-K: 3                        │
│  └─ 结果: 项目需求文档 3 条         │
│                                     │
└─────────────────────────────────────┘
    ↓
合并结果：总计 6 条证据（3 标准 + 3 SRD）
```

## 核心实现

### 1. indexing.py - 新增双路检索函数

```python
def retrieve_context_dual(
    index: VectorStoreIndex,
    query: str,
    *,
    standards_top_k: int = 3,
    srd_top_k: int = 3,
) -> list[dict]:
    """双路检索：分别从标准文档和 SRD 文档中检索"""
    
    # 泳道 1: 标准文档
    std_filters = MetadataFilters(
        filters=[ExactMatchFilter(key="category", value="Standards")]
    )
    standard_retriever = VectorIndexRetriever(
        index=index,
        filters=std_filters,
        similarity_top_k=standards_top_k
    )
    std_nodes = standard_retriever.retrieve(query)
    
    # 泳道 2: SRD 文档
    srd_filters = MetadataFilters(
        filters=[ExactMatchFilter(key="category", value="SRD_Context")]
    )
    srd_retriever = VectorIndexRetriever(
        index=index,
        filters=srd_filters,
        similarity_top_k=srd_top_k
    )
    srd_nodes = srd_retriever.retrieve(query)
    
    # 合并结果
    return std_nodes + srd_nodes
```

### 2. pipeline.py - 修改 retrieve 函数

```python
def retrieve(state: RagState) -> RagState:
    for ar in atomic_requirements:
        # 动态分配检索数量
        standards_k = max(2, ms.top_k // 2)  # 至少 2 条标准证据
        srd_k = max(2, ms.top_k - standards_k)  # 剩余分配给 SRD
        
        # 使用双路检索
        results = retrieve_context_dual(
            index, 
            ar.statement, 
            standards_top_k=standards_k,
            srd_top_k=srd_k
        )
```

### 3. streamlit_app.py - 显示证据来源

```python
def _render_evidence(ev: list[dict]):
    for e in ev:
        category = e.get("category", "Unknown")
        category_label = "📘 标准" if category == "Standards" else "📄 SRD"
        st.markdown(f"**{category_label} | {ref}** | score={score}")
```

## 检索数量分配策略

| UI 设置 Top-K | 标准文档 | SRD 文档 | 原始总计 | 过滤后（约） |
|--------------|---------|---------|---------|-------------|
| 3            | 3       | 3       | 6       | 4-6         |
| 4            | 4       | 4       | 8       | 6-8         |
| 6            | 6       | 6       | 12      | 8-12        |
| 8            | 8       | 8       | 16      | 12-16       |
| 10           | 10      | 10      | 20      | 15-20       |

**分配逻辑：**
```python
# 固定模式：每个泳道都检索 top_k 条
standards_top_k = ms.top_k  # 标准文档：top_k 条
srd_top_k = ms.top_k        # SRD 文档：top_k 条
# 原始总计：top_k * 2 条
# 过滤后（score >= 0.45）：实际数量会减少
```

## 效果对比

### 单路检索（旧）
```
查询："起落架控制逻辑"
Top-K = 3

结果：
[1] 📄 SRD | 项目需求文档.docx | score=0.89
[2] 📄 SRD | 项目需求文档.docx | score=0.85
[3] 📄 SRD | 项目需求文档.docx | score=0.82

❌ 问题：全部来自 SRD，缺少标准文档证据
```

### 双路检索（新）
```
查询："起落架控制逻辑"
UI 设置 Top-K = 3

双路检索：
  Standards Top-K = 3
  SRD Top-K = 3

原始结果（6 条）：
[1] 📘 标准 | DO-178C.pdf | p.45 | score=0.87
[2] 📘 标准 | ARP4754A.pdf | p.62 | score=0.83
[3] 📘 标准 | IEEE 829.pdf | p.12 | score=0.42  ← 被过滤（< 0.45）
[4] 📄 SRD | 项目需求文档.docx | score=0.89
[5] 📄 SRD | 项目需求文档.docx | score=0.85
[6] 📄 SRD | 系统架构文档.docx | score=0.38  ← 被过滤（< 0.45）

过滤后（4 条）：
[1] 📘 标准 | DO-178C.pdf | p.45 | score=0.87
[2] 📘 标准 | ARP4754A.pdf | p.62 | score=0.83
[3] 📄 SRD | 项目需求文档.docx | score=0.89
[4] 📄 SRD | 项目需求文档.docx | score=0.85

✅ 优势：
  - 两类证据都有覆盖
  - 总数量更多（原始 6 条，过滤后 4 条）
  - 质量有保证（score >= 0.45）
```

## 优势

1. **保证覆盖**：确保标准文档和 SRD 文档的证据都被检索到
2. **避免偏向**：防止某一类文档因相似度高而"霸占"所有结果
3. **数量更多**：原始检索 `top_k * 2` 条，过滤后仍有足够证据
4. **独立优化**：可以针对不同类型文档使用不同的检索策略

## 注意事项

1. **总数量翻倍**：原始检索数量是 `top_k * 2`（如 top_k=3 时检索 6 条）
2. **过滤会减少**：经过 `score >= 0.45` 过滤后，实际数量会减少
3. **过滤失败**：如果某个泳道没有结果（如 SRD 为空），不会报错，只返回另一个泳道的结果
4. **窗口扩展**：标准文档仍然使用句子窗口扩展，SRD 文档使用原始分块

## 后续优化方向

1. **动态权重**：根据查询类型自动调整两类文档的检索比例
2. **混合排序**：将两类结果按相似度重新排序
3. **重排序模型**：使用 Reranker 对合并结果进行精排
4. **自适应分配**：根据历史检索效果动态调整分配策略
