# 句子窗口检索（Sentence Window Retrieval）实现说明

## 改动概述

已将原有的固定大小分块（SentenceSplitter）替换为句子窗口检索（SentenceWindowNodeParser），提升航空标准文档的检索精度。

## 核心改动

### 1. settings.py
- 新增参数 `sentence_window_size: int = 4`
- 默认前后各扩展 4 句，可在 UI 中动态调整（2-8 句）

### 2. indexing.py
- 替换分块器：`SentenceWindowNodeParser` 替代 `SentenceSplitter`
- 配置窗口元数据键：
  - `window_metadata_key="window"` - 存储扩展后的上下文
  - `original_text_metadata_key="original_sentence"` - 存储原始匹配句子
- 更新 `retrieve_context()` 函数：
  - **关键修复**：使用 `MetadataReplacementPostProcessor` 后处理器
  - 自动将 `node.get_content()` 替换为 `metadata["window"]`
  - 避免"原始短句 + window"的重复叠加问题（常见坑点）

### 3. streamlit_app.py
- 侧边栏新增"Window（句子窗口大小）"滑块（2-8 句）
- 添加提示文字："✨ 使用句子窗口检索：精准匹配单句，扩展上下文"
- 支持动态调整 Top-K 检索数量（2-8 条）

## 工作原理

```
原始文档：
  句子1. 起落架控制手柄应在DOWN位置。
  句子2. 飞行速度必须低于250节。
  句子3. 执行机构应在3秒内接收到指令。
  句子4. 超速时触发告警。

索引构建：
  - 每个句子单独建立向量索引（精准匹配）
  - 同时保存前后N句作为窗口上下文

检索时：
  查询："起落架速度限制"
  ↓
  匹配到：句子2（相似度最高）
  ↓
  后处理器替换：node.content = metadata["window"]
  ↓
  返回：句子1 + 句子2 + 句子3（窗口大小=1时）
  
  ⚠️ 关键：使用 MetadataReplacementPostProcessor 确保只返回窗口文本，
     避免"原始短句 + window"的重复叠加（LlamaIndex 常见坑点）
```

## 优势

1. **精准度高**：单句匹配避免无关内容混入
2. **上下文完整**：窗口扩展保证语义连贯
3. **适配标准文档**：DO-178C、IEEE 829 等条款式文档的最佳实践
4. **灵活可调**：窗口大小可根据文档特点动态调整

## 使用方法

### 首次运行（重建索引）
```bash
streamlit run streamlit_app.py
```
在侧边栏勾选"重建向量库"，系统会使用新的句子窗口策略重新索引。

### 测试检索效果
```bash
python test_sentence_window.py
```

### 调优建议
- **窗口大小 = 2-3**：适合条款密集、每句独立的标准文档（DO-178C、arp4754）
- **窗口大小 = 4-5**：适合叙述性较强的需求文档（SRD）
- **窗口大小 = 6-8**：适合需要更多上下文的复杂场景
- **Top-K = 3**：默认推荐，平衡精度和召回率
- **Top-K = 5-8**：复杂需求，需要更多证据支撑

## 注意事项

1. **必须重建索引**：修改分块策略后，旧的向量库不兼容，需要勾选"重建向量库"
2. **索引时间**：句子窗口会增加节点数量，首次索引可能需要更长时间
3. **存储空间**：每个句子都存储窗口上下文，向量库体积会略微增加

## 后续优化方向

1. **双路检索（已实现）** ✅
   - 标准文档（Standards）：句子窗口检索
   - SRD 文档：固定大块检索（512 tokens）
   - 分别检索，合并结果

2. **混合检索**：结合 BM25 关键词匹配 + 向量检索
3. **重排序**：使用 Reranker 模型对 Top-K 结果重新排序
4. **自适应窗口**：根据文档类型自动调整窗口大小
5. **高亮显示**：UI 中高亮显示原始匹配句子

## 技术参考

- LlamaIndex 官方文档：[SentenceWindowNodeParser](https://docs.llamaindex.ai/en/stable/api_reference/node_parsers/sentence_window/)
- 论文参考：Sentence-BERT 和 Dense Passage Retrieval (DPR)
- 双路检索策略：针对不同文档类型采用不同分块策略

## 更新日志

### 2025-04-18
- ✅ 实现双路检索：标准文档（句子窗口）+ SRD 文档（固定大块）
- ✅ 优化窗口大小默认值为 4 句
- ✅ 支持前端动态调整 Top-K 和窗口大小
- ✅ 添加分数阈值过滤（0.45），提升证据质量

### 初始版本
- ✅ 实现句子窗口检索替代固定分块
- ✅ 使用 MetadataReplacementPostProcessor 避免内容重复
- ✅ 支持动态窗口大小调整
